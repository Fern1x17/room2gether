// Implementación por defecto de [openExternalLink] para plataformas no web
// (Android/iOS). No hace nada: las pantallas deben usar `kIsWeb` para dar una
// alternativa en móvil hasta que se integre un lanzador de enlaces nativo.
void openExternalLink(String url) {}
